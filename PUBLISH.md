# Publish Configuration for `zikzak_session`

<!-- Auto-generated by publish-manager skill on 2026-08-27. Edit if the workflow changes. -->

## Package Manager

- **Type**: `pub.dev` (Dart package, library)
- **Packages**: `zikzak_session` (single package at repo root)
- **Public packages**: `zikzak_session` → https://pub.dev/packages/zikzak_session
- **Private packages**: none

## Critical: path dependency must be converted before publish

The repo's working `pubspec.yaml` uses a **local path dependency** for `zuraffa`:

```yaml
dependencies:
  zuraffa:
    path: ../zuraffa
```

pub.dev **rejects** path dependencies. Before publishing, convert it to the hosted
constraint (the published version is `6.0.0` as of this writing):

```yaml
dependencies:
  zuraffa: ^6.0.0
```

The reverse conversion (path dep) is the normal dev setup — keep `../zuraffa` on the
`main`/`feat` branches, only switch to hosted on the release branch.

Run `dart pub get` after the swap so `pubspec.lock` re-resolves.

## Other dependency notes

- `get_it` is imported directly by `test/session_transport_test.dart`, so it must be
  declared in `dev_dependencies` (current: `^9.2.1`) or the dry-run warns.
- `pubspec.lock` is gitignored in this repo (library convention) — it is not committed.

## Scripts

- **Pre-publish**: none — performed manually (version bump + `zuraffa` path→hosted swap).
- **Publish**: none — run `dart pub publish --force` directly from the release branch.
- **Post-publish**: none — manual merge / revert as needed.
- **Restore dev**: none — manually restore `zuraffa: path: ../zuraffa` on the dev branch.

## Workflow

1. Update `CHANGELOG.md` (use changelog-manager skill: prepend `## X.Y.Z - YYYY-MM-DD`).
2. Create release branch: `git checkout -b publish-<version>`.
3. Bump `version:` in `pubspec.yaml`; convert `zuraffa` path dep → `^6.0.0`; `dart pub get`.
4. Validate: `dart pub publish --dry-run` and `dart analyze` (expect only the
   "uncommitted files" warning).
5. Commit (`Prepare for publishing version <version>`) and `git push -u origin publish-<version>`.
6. Publish: `dart pub publish --force`.
7. Tag and push: `git tag <version> && git push origin <version>`.
8. Restore dev `zuraffa` path dependency on the main/feat branch as appropriate.

## Notes

- No tags existed before `0.2.0`; tag format is the bare semver (e.g. `0.2.0`), no `v` prefix.
- The publish is irreversible on pub.dev; once `dart pub publish` succeeds the version is permanent.
- After upload, the new version can take up to ~10 minutes to appear on pub.dev.
